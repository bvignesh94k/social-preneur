# Admin login. Generate the hash and secret with:
#   node scripts/hash-password.mjs "your-password"
ADMIN_EMAIL=
ADMIN_PASSWORD_HASH=
SESSION_SECRET=

# Postgres, added automatically when the Neon database is created in Vercel
# DATABASE_URL=
